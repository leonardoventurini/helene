import {
  Counter
} from '../components/counter.jsx'

This text is written in Markdown.

MDX allows Rich React components to be used directly in Markdown:

<Counter/>

```jsx
import {
  Counter
} from './counter';

<Counter />
```
